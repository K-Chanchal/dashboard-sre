version: 1
frontend:
  phases:
    # No build phase needed since we're using vanilla HTML/CSS/JS
    preBuild:
      commands:
        - echo "No build required for static frontend"
    build:
      commands:
        - echo "Deploying static files..."
  artifacts:
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths: []
